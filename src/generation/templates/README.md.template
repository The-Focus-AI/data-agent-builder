# {{SERVER_NAME}} MCP Server

MCP server for querying and analyzing {{DESCRIPTION}}.

## Features

{{FEATURES}}

## Installation

```bash
npm install
npm run build
```

## Usage

### With Claude Desktop

Add this to your Claude Desktop configuration:

```json
{
  "mcpServers": {
    "{{CONFIG_NAME}}": {
      "command": "node",
      "args": ["/path/to/{{PACKAGE_NAME}}/dist/index.js"],
      "env": {
        "DATA_PATH": "/path/to/your/data.db"
      }
    }
  }
}
```

### With Other MCP Clients

The server runs on stdio and can be used with any MCP-compatible client.

## Available Tools

{{TOOL_DOCUMENTATION}}

## Data Schema

This server expects data with the following structure:

{{SCHEMA_DOCUMENTATION}}

## Loading New Data

To load new data files with the same structure:

```bash
# The server expects a SQLite database at DATA_PATH
# Or you can use the load_data tool to import CSV files
```

## Development

```bash
# Watch mode for development
npm run watch

# Build for production
npm run build
```

## Generated by Data Agent Builder

This MCP server was automatically generated by analyzing your sample data and learning its structure.
The server is configured to work with data files that match the analyzed schema.
